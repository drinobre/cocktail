export default function List(drinks) {
  drinks.map((drink) => {
    return (
      <li>
        {drink.concat((i) => {
          for (i = 0; i <= 0; i++) {
            return i;
          }
        })}
      </li>
    );
  });
}
